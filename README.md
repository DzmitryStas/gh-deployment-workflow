# gh-deployment-workflow
# GitHub Pages Deployment

This project demonstrates automatic deployment of a static website to GitHub Pages
using a GitHub Actions workflow.

## What it does

- Uses GitHub Actions to deploy `index.html` to GitHub Pages
- Triggers only when `index.html` changes on the `main` branch
- 
